[project]
name = "simcore-ai-django"
version = "0.4.0"
description = "Django integration for simcore-ai (signals, adapters, autodiscovery)"
readme = "README.md"
authors = [
    { name = "Tyler J", email = "tyler@jackfruit.me" }
]
requires-python = ">=3.14"
dependencies = [
  "django>=5.2",
  "simcore-ai>=0.2.0",
]

[project.optional-dependencies]
celery = ["celery>=5.4"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/simcore_ai_django"]

[tool.hatch.build.targets.sdist]
include = [
    "src/simcore_ai_django/**",
    "tests/simcore_ai_django/**",
    "pyproject.toml",
    "README.md",
    "LICENSE*",
]